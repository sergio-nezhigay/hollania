{%- if cart.item_count > 0 -%}
  <div
      class="absolute right-[15%] top-[12%] bg-red-600 text-white w-4 h-4 rounded-full leading-relaxed flex
          justify-center items-center font-semibold font-Inter text-[10px]">
    {{ cart.item_count }}
  </div>
{%- endif -%}
